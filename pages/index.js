// Sintaxe para componentes React: 1ª letra de cada palavra maiuscula
// Todo componente React se escreve igual uma função JS


/**
 *  Componente React para a Home
 */ 
function Home() {
    return <h1>Bem-vindo :)</h1>
}

// Para saber qual função o Next deve retornar para renderizar a página, é necessário utilizar esta linha:
export default Home; // export (exportar para fora do arquivo) default (é para definir o padrão do arquivo) NomeDaFunção